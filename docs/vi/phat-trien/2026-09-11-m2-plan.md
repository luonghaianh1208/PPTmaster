# M2 — Trợ lý hỏi đáp cho thầy cô (`v6.3.2-vi.2`): Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm bộ file hướng dẫn tiếng Việt để AI hỏi thầy cô một lượt ngắn (5 loại việc), ghi brief vào `sources/` của dự án rồi mới chạy quy trình tạo PPTX của upstream; phát hành `v6.3.2-vi.2`.

**Architecture:** Lớp Việt chỉ **bổ sung** tài liệu nguồn và ngữ cảnh: `AGENTS.vi.md` §10 trỏ tới `docs/vi/tro-ly/quy-trinh-hoi.md` và 5 file loại việc; AI hỏi → ghi `projects/_brief-<tên>.md` → import vào `sources/` → bước xác nhận upstream (điền sẵn, trong khung chat). Không file nào dưới `skills/` bị sửa; test cấu trúc trong `tools/vi/tests/test_vi_layer.py` giữ khuôn các file hướng dẫn.

**Tech Stack:** Markdown (tiếng Việt), Python ≥ 3.10 stdlib `unittest`, Claude Code CLI (kiểm tra hành vi headless), git, `gh`.

**Spec:** [2026-09-11-tro-ly-hoi-dap-design.md](2026-09-11-tro-ly-hoi-dap-design.md)

## Global Constraints

- Nhánh làm việc: `feat/vi-tro-ly` (tách từ `main` @ `75562b38`). Upstream mới nhất vẫn là `v6.3.2`.
- Không sửa bất kỳ file nào dưới `skills/`: `git diff v6.3.2 -- skills/` phải rỗng; `python skills/ppt-master/scripts/attribution_guard.py` exit 0.
- File được tạo/sửa trong M2: `docs/vi/tro-ly/*` (8 file), `AGENTS.vi.md`, `docs/vi/bat-dau-nhanh.md`, `docs/vi/cau-lenh-mau.md`, `README.md`, `CHANGELOG-VI.md`, `tools/vi/tests/test_vi_layer.py`, `docs/vi/phat-trien/2026-09-11-m2-kiem-thu-headless.md`. Không cần mở rộng `ALLOWED_CHANGES`.
- File Markdown viết hoàn toàn bằng tiếng Việt; chuỗi không phải tiếng Việt chỉ xuất hiện ở lệnh, đường dẫn, tên nút/menu, key kỹ thuật và câu lệnh mẫu của người dùng.
- 5 file loại việc (`bai-giang.md`, `bao-cao-tong-ket.md`, `hoat-dong-doan.md`, `poster-mang-xa-hoi.md`, `tap-huan-workshop.md`): đúng 8 tiêu đề cấp 2 theo thứ tự `## Khi nào dùng`, `## Câu hỏi bắt buộc`, `## Câu hỏi tuỳ chọn`, `## Tạo nhanh`, `## Cấu trúc gợi ý`, `## Phong cách gợi ý`, `## Khổ slide`, `## Ghi vào brief`; `## Câu hỏi bắt buộc` có 1–7 mục đánh số, mỗi mục có "Gợi ý:"; `## Tạo nhanh` có 2–3 mục đánh số; trong `## Khổ slide` chỉ đặt key canvas trong dấu backtick.
- 5 file loại việc và 2 file mẫu **không** chứa link Markdown (dùng đường dẫn trong backtick); chỉ `quy-trinh-hoi.md` và `AGENTS.vi.md` được link sang các file này.
- Trong `docs/vi/tro-ly/*`, dòng bắt đầu bằng `## ` chỉ dùng cho tiêu đề mục thật (không đặt trong khối code).
- Test: `unittest` chuẩn trong `tools/vi/tests/`; chạy `& $PY -m unittest discover -s tools/vi/tests -v` từ thư mục gốc repo.
- `$PY` = `C:\Users\ADMIN\AppData\Local\Temp\claude\c--Users-ADMIN-Downloads-VIBE-CODING-PPTmaster\c49c4a0d-7ba3-41e7-84e8-5f1e4b4ad88f\scratchpad\venv312\Scripts\python.exe` (định nghĩa lại trong mỗi lần gọi PowerShell).
- Commit theo Conventional Commits; message kết thúc bằng một dòng `Co-Authored-By: Claude … <noreply@anthropic.com>` theo hướng dẫn ghi công của phiên đang thực hiện.
- Không push trước Task 7; không bao giờ force push; mọi lệnh `gh` đều có `--repo luonghaianh1208/PPTmaster`.
- Kiểm thử không được mở cửa sổ ứng dụng hay trình duyệt.

## File Structure

| File | Trách nhiệm |
|---|---|
| `docs/vi/tro-ly/mau-ho-so-don-vi.md` | Mẫu hồ sơ đơn vị (7 mục) |
| `docs/vi/tro-ly/mau-brief.md` | Mẫu brief (6 mục cấp 2) |
| `docs/vi/tro-ly/bai-giang.md` | Bộ câu hỏi + gợi ý cho bài giảng |
| `docs/vi/tro-ly/bao-cao-tong-ket.md` | Bộ câu hỏi + gợi ý cho báo cáo – tổng kết |
| `docs/vi/tro-ly/hoat-dong-doan.md` | Bộ câu hỏi + gợi ý cho hoạt động Đoàn – sự kiện |
| `docs/vi/tro-ly/poster-mang-xa-hoi.md` | Bộ câu hỏi + gợi ý cho poster Zalo – Facebook |
| `docs/vi/tro-ly/tap-huan-workshop.md` | Bộ câu hỏi + gợi ý cho tập huấn/workshop |
| `docs/vi/tro-ly/quy-trinh-hoi.md` | Quy tắc chung: nhận diện, hồ sơ, cách hỏi, brief, import, tạo nhanh, đổi ý |
| `AGENTS.vi.md` (mục 10) | Điểm vào cho AI: khi nào đọc bộ hướng dẫn |
| `docs/vi/bat-dau-nhanh.md`, `docs/vi/cau-lenh-mau.md`, `README.md` | Giải thích cho người dùng |
| `tools/vi/tests/test_vi_layer.py` | Test cấu trúc bộ hướng dẫn |
| `docs/vi/phat-trien/2026-09-11-m2-kiem-thu-headless.md` | Kết quả 7 ca kiểm tra hành vi |
| `CHANGELOG-VI.md` | Mục `6.3.2-vi.2` (Task 7) |

Thứ tự task: 1 mẫu + 5 file loại việc → 2 quy trình chung → 3 `AGENTS.vi.md` → 4 tài liệu người dùng → 5 kiểm tra hành vi headless → 6 nghiệm thu chủ repo → 7 phát hành.

---

### Task 1: Mẫu hồ sơ, mẫu brief và 5 file loại việc

**Files:**
- Create: `docs/vi/tro-ly/mau-ho-so-don-vi.md`, `docs/vi/tro-ly/mau-brief.md`
- Create: `docs/vi/tro-ly/bai-giang.md`, `docs/vi/tro-ly/bao-cao-tong-ket.md`, `docs/vi/tro-ly/hoat-dong-doan.md`, `docs/vi/tro-ly/poster-mang-xa-hoi.md`, `docs/vi/tro-ly/tap-huan-workshop.md`
- Test: `tools/vi/tests/test_vi_layer.py`

**Interfaces:**
- Consumes: trong `test_vi_layer.py` đã có `REPO_ROOT`, `read(relative) -> str`, `canvas_format_keys() -> set`, `re`.
- Produces (thêm vào `test_vi_layer.py`, trước khối `if __name__ == "__main__":`): hằng `TRO_LY_DIR: Path`, `GUIDE_FILES: tuple[str, ...]`, `GUIDE_HEADINGS: tuple[str, ...]`, `PROFILE_FIELDS: tuple[str, ...]`, `BRIEF_HEADINGS: tuple[str, ...]`, `NUMBERED_RE`; hàm `h2_headings(text: str) -> list[str]`, `section(text: str, heading: str) -> str`, `numbered_items(body: str) -> list[str]`; class `TeacherAssistantTemplatesTest`, `TeacherAssistantGuidesTest`. Task 2–4 dùng lại các hàm này.

- [ ] **Step 1: Viết test thất bại**

Thêm vào `tools/vi/tests/test_vi_layer.py`, ngay trước `if __name__ == "__main__":`:

```python
TRO_LY_DIR = REPO_ROOT / "docs" / "vi" / "tro-ly"
GUIDE_FILES = (
    "bai-giang.md",
    "bao-cao-tong-ket.md",
    "hoat-dong-doan.md",
    "poster-mang-xa-hoi.md",
    "tap-huan-workshop.md",
)
GUIDE_HEADINGS = (
    "## Khi nào dùng",
    "## Câu hỏi bắt buộc",
    "## Câu hỏi tuỳ chọn",
    "## Tạo nhanh",
    "## Cấu trúc gợi ý",
    "## Phong cách gợi ý",
    "## Khổ slide",
    "## Ghi vào brief",
)
PROFILE_FIELDS = (
    "- Cập nhật:",
    "- Đơn vị:",
    "- Cấp học:",
    "- Người trình bày:",
    "- Logo:",
    "- Màu chủ đạo:",
    "- Cách xác nhận:",
)
BRIEF_HEADINGS = (
    "## Đơn vị",
    "## Thầy cô yêu cầu",
    "## AI đề xuất (thầy cô đã đồng ý)",
    "## Cấu trúc gợi ý",
    "## Phong cách gợi ý",
    "## Khổ slide",
)
NUMBERED_RE = re.compile(r"^\d+\. ")


def h2_headings(text: str) -> list:
    return [line.strip() for line in text.splitlines() if line.startswith("## ")]


def section(text: str, heading: str) -> str:
    lines = text.splitlines()
    starts = [index for index, line in enumerate(lines) if line.strip() == heading]
    if not starts:
        raise AssertionError(f"Thiếu mục: {heading}")
    body = []
    for line in lines[starts[0] + 1:]:
        if line.startswith("## "):
            break
        body.append(line)
    return "\n".join(body)


def numbered_items(body: str) -> list:
    items = []
    current = None
    for line in body.splitlines():
        if NUMBERED_RE.match(line):
            if current is not None:
                items.append(current)
            current = line
        elif current is not None:
            current += "\n" + line
    if current is not None:
        items.append(current)
    return items


class TeacherAssistantTemplatesTest(unittest.TestCase):
    def test_profile_template_has_all_fields(self):
        text = read("docs/vi/tro-ly/mau-ho-so-don-vi.md")
        for field in PROFILE_FIELDS:
            self.assertIn(field, text)

    def test_brief_template_has_exact_sections(self):
        self.assertEqual(h2_headings(read("docs/vi/tro-ly/mau-brief.md")), list(BRIEF_HEADINGS))


class TeacherAssistantGuidesTest(unittest.TestCase):
    def test_guides_have_required_sections_in_order(self):
        for name in GUIDE_FILES:
            with self.subTest(guide=name):
                self.assertEqual(h2_headings(read(f"docs/vi/tro-ly/{name}")), list(GUIDE_HEADINGS))

    def test_required_questions_are_limited_and_have_suggestions(self):
        for name in GUIDE_FILES:
            with self.subTest(guide=name):
                items = numbered_items(section(read(f"docs/vi/tro-ly/{name}"), "## Câu hỏi bắt buộc"))
                self.assertTrue(1 <= len(items) <= 7, f"{len(items)} câu")
                for item in items:
                    self.assertIn("Gợi ý:", item)

    def test_quick_mode_asks_two_or_three_questions(self):
        for name in GUIDE_FILES:
            with self.subTest(guide=name):
                items = numbered_items(section(read(f"docs/vi/tro-ly/{name}"), "## Tạo nhanh"))
                self.assertTrue(2 <= len(items) <= 3, f"{len(items)} câu")

    def test_slide_formats_exist_upstream(self):
        keys = canvas_format_keys()
        for name in GUIDE_FILES:
            with self.subTest(guide=name):
                used = re.findall(r"`([a-z0-9_]+)`", section(read(f"docs/vi/tro-ly/{name}"), "## Khổ slide"))
                self.assertTrue(used, "Chưa nêu khổ slide")
                self.assertEqual(sorted(set(used) - keys), [])

    def test_guides_and_templates_have_no_markdown_links(self):
        for name in GUIDE_FILES + ("mau-ho-so-don-vi.md", "mau-brief.md"):
            with self.subTest(file=name):
                self.assertEqual(LINK_RE.findall(read(f"docs/vi/tro-ly/{name}")), [])
```

- [ ] **Step 2: Chạy test, xác nhận thất bại**

Run: `& $PY -m unittest discover -s tools/vi/tests -p "test_vi_layer.py" -v`
Expected: các test `TeacherAssistant*` ERROR (`FileNotFoundError` cho `docs/vi/tro-ly/...`); các test cũ vẫn OK.

- [ ] **Step 3: Tạo `docs/vi/tro-ly/mau-ho-so-don-vi.md`**

```markdown
# Hồ sơ đơn vị

> Mẫu dành cho AI. Bản dùng thật lưu ở `projects/_ho-so-don-vi.md` trên máy người dùng. Thay phần trong ngoặc nhọn bằng thông tin thật.

- Cập nhật: <YYYY-MM-DD>
- Đơn vị: <tên trường hoặc đơn vị>
- Cấp học: <Tiểu học | THCS | THPT | khác>
- Người trình bày: <họ tên> — <chức danh>
- Logo: <đường dẫn file logo, hoặc "không dùng">
- Màu chủ đạo: <mã màu như #1F4E79, hoặc mô tả>
- Cách xác nhận: <khung chat | trang web>
```

- [ ] **Step 4: Tạo `docs/vi/tro-ly/mau-brief.md`**

```markdown
# Tóm tắt yêu cầu từ thầy cô

> Mẫu dành cho AI. Bản dùng thật lưu ở `projects/_brief-<tên_dự_án>.md`, sau đó được import vào thư mục `sources/` của dự án. Thay phần trong ngoặc nhọn bằng thông tin thật.

- Loại việc: <Bài giảng | Báo cáo – tổng kết | Hoạt động Đoàn – sự kiện | Poster/ấn phẩm Zalo – Facebook | Tập huấn/workshop>
- Ngày: <YYYY-MM-DD>

## Đơn vị

<chép các mục cần dùng từ hồ sơ đơn vị: đơn vị, cấp học, người trình bày, logo, màu chủ đạo>

## Thầy cô yêu cầu

<ghi đúng lời thầy cô, mỗi ý một dòng; điều cấm ghi nguyên văn>

## AI đề xuất (thầy cô đã đồng ý)

<các gợi ý thầy cô chấp nhận; mục thầy cô không trả lời ghi kèm "(AI đề xuất)">

## Cấu trúc gợi ý

<mạch trang gợi ý; đây là điểm khởi đầu, không phải bố cục cuối, trừ khi thầy cô yêu cầu giữ nguyên>

## Phong cách gợi ý

<màu, font, cỡ chữ, và lựa chọn của upstream nên đề xuất>

## Khổ slide

<key canvas, ví dụ ppt169>
```

- [ ] **Step 5: Tạo `docs/vi/tro-ly/bai-giang.md`**

```markdown
# Loại việc: Bài giảng

File dành cho AI. Luôn đọc `docs/vi/tro-ly/quy-trinh-hoi.md` trước file này.

## Khi nào dùng

Thầy cô cần slide cho một bài dạy hoặc tiết học, ở bất kỳ môn và cấp học nào.

Ví dụ câu lệnh:
- "Tạo bài giảng Vật lí 10 bài Chuyển động thẳng đều"
- "Làm slide cho tiết Ngữ văn 7 bài Bầy chim chìa vôi"

## Câu hỏi bắt buộc

1. Môn, lớp, tên bài, và bài thuộc chủ đề hoặc chương nào?
   Gợi ý: lấy từ câu lệnh nếu đã có; chỉ hỏi phần còn thiếu.
2. Thầy cô dạy theo bộ sách nào: Kết nối tri thức với cuộc sống, Chân trời sáng tạo, Cánh diều, hay bộ khác?
   Gợi ý: nếu hồ sơ hoặc lần trước đã có bộ sách thì đề xuất lại bộ đó.
3. Bài dạy trong bao nhiêu tiết, mỗi tiết bao nhiêu phút?
   Gợi ý: 1 tiết × 45 phút.
4. Mục tiêu về kiến thức, năng lực và phẩm chất của bài là gì?
   Gợi ý: AI đề xuất 2–3 ý cho mỗi nhóm theo chương trình giáo dục phổ thông 2018 để thầy cô sửa.
5. Thầy cô muốn có những hoạt động nào: trò chơi khởi động, thảo luận nhóm, phiếu học tập, câu hỏi trắc nghiệm, video?
   Gợi ý: một trò chơi khởi động ngắn và 3–5 câu trắc nghiệm ở phần luyện tập.
6. Học sinh của lớp có đặc điểm gì, và phòng học dùng thiết bị gì (máy chiếu 16:9, máy chiếu 4:3, TV)?
   Gợi ý: lớp có trình độ trung bình, dùng máy chiếu 16:9.
7. Thầy cô có tài liệu sẵn không (giáo án Word, ảnh trong sách giáo khoa, đề bài)?
   Gợi ý: không có; AI soạn từ nội dung bài học.

## Câu hỏi tuỳ chọn

- Lớp có học sinh cần hỗ trợ riêng không? Chỉ hỏi khi thầy cô nhắc tới học sinh khuyết tật hoặc học sinh cần kèm cặp.
- Có cần bản in phiếu học tập cho học sinh không? Chỉ hỏi khi thầy cô nhắc tới phiếu học tập.

## Tạo nhanh

1. Môn, lớp và tên bài (câu hỏi bắt buộc 1).
2. Bộ sách (câu hỏi bắt buộc 2).

## Cấu trúc gợi ý

- THCS và THPT: theo 4 hoạt động của Công văn 5512/BGDĐT-GDTrH — Mở đầu → Hình thành kiến thức mới → Luyện tập → Vận dụng. Thêm trang bìa và trang mục tiêu ở đầu, trang tổng kết và dặn dò ở cuối.
- Tiểu học: theo mạch tiết học thầy cô quen dùng; không áp khung 4 hoạt động nếu thầy cô không yêu cầu.
- Mỗi hoạt động nêu rõ nhiệm vụ của học sinh. Câu hỏi trắc nghiệm đặt đáp án ở slide riêng ngay sau câu hỏi.

## Phong cách gợi ý

- Học đường, sáng rõ, nền sáng, tương phản cao để học sinh cuối lớp vẫn đọc được.
- Chữ nội dung từ 28pt; font Segoe UI hoặc Arial.
- Đề xuất mode `instructional` của upstream.
- Khi dùng máy chiếu 4:3 có thể đề xuất layout `presentation_core_43` của upstream.

## Khổ slide

- Mặc định `ppt169`.
- Dùng `ppt43` khi thầy cô dùng máy chiếu 4:3.

## Ghi vào brief

- Loại việc: Bài giảng.
- Thầy cô yêu cầu: môn, lớp, tên bài, bộ sách, số tiết, các hoạt động thầy cô chọn, đặc điểm học sinh, thiết bị, tài liệu có sẵn — ghi đúng lời thầy cô.
- AI đề xuất (thầy cô đã đồng ý): mục tiêu và các gợi ý thầy cô chấp nhận.
- Cấu trúc gợi ý, Phong cách gợi ý, Khổ slide: theo các mục tương ứng của file này.
- Viết theo mẫu `docs/vi/tro-ly/mau-brief.md`.
```

- [ ] **Step 6: Tạo `docs/vi/tro-ly/bao-cao-tong-ket.md`**

```markdown
# Loại việc: Báo cáo – tổng kết

File dành cho AI. Luôn đọc `docs/vi/tro-ly/quy-trinh-hoi.md` trước file này.

## Khi nào dùng

Thầy cô hoặc bộ phận cần slide trình bày báo cáo: sơ kết, tổng kết, chuyên đề, thi đua.

Ví dụ câu lệnh:
- "Làm slide báo cáo tổng kết năm học của tổ Toán"
- "Từ file bao-cao.docx tạo slide sơ kết học kỳ I"

## Câu hỏi bắt buộc

1. Đây là loại báo cáo nào: sơ kết học kỳ, tổng kết năm học, báo cáo chuyên đề hay báo cáo thi đua?
   Gợi ý: lấy từ câu lệnh nếu đã nêu.
2. Kỳ báo cáo là khi nào, và đơn vị hoặc bộ phận nào báo cáo?
   Gợi ý: năm học hiện tại; đơn vị lấy từ hồ sơ.
3. Ai là người nghe: hội nghị viên chức, cha mẹ học sinh, hay đoàn kiểm tra cấp trên?
   Gợi ý: hội nghị viên chức của trường.
4. Thời lượng trình bày bao lâu?
   Gợi ý: 15 phút, khoảng 12–15 slide.
5. Số liệu lấy từ đâu (file Excel hoặc Word đính kèm), và số liệu nào cần vẽ biểu đồ?
   Gợi ý: dùng file thầy cô gửi; vẽ biểu đồ cho tỉ lệ xếp loại và kết quả thi.
6. Nội dung nào cần nhấn mạnh: thành tích nổi bật, hạn chế cần nói thẳng?
   Gợi ý: 3 thành tích nổi bật và 2 hạn chế chính.

## Câu hỏi tuỳ chọn

- Có cần trang so sánh với năm trước không? Chỉ hỏi khi số liệu có từ hai kỳ trở lên.

## Tạo nhanh

1. Loại báo cáo và kỳ báo cáo (câu hỏi bắt buộc 1 và 2).
2. File số liệu (câu hỏi bắt buộc 5).

## Cấu trúc gợi ý

- Trang bìa → Kết quả đạt được → Hạn chế, nguyên nhân → Bài học kinh nghiệm → Phương hướng, nhiệm vụ → Kiến nghị, đề xuất → Trang cảm ơn.
- Mỗi trang kết quả có một con số hoặc biểu đồ chính.

## Phong cách gợi ý

- Hành chính, trang trọng, bố cục gọn.
- Font Times New Roman theo thể thức văn bản hành chính (Nghị định 30/2020/NĐ-CP).
- Màu chủ đạo theo hồ sơ đơn vị; biểu đồ ghi rõ số liệu và đơn vị tính.

## Khổ slide

- Mặc định `ppt169`.

## Ghi vào brief

- Loại việc: Báo cáo – tổng kết.
- Thầy cô yêu cầu: loại báo cáo, kỳ báo cáo, đơn vị, người nghe, thời lượng, nguồn số liệu, nội dung cần nhấn mạnh — ghi đúng lời thầy cô.
- AI đề xuất (thầy cô đã đồng ý): số slide, các biểu đồ đề xuất và các gợi ý thầy cô chấp nhận.
- Cấu trúc gợi ý, Phong cách gợi ý, Khổ slide: theo các mục tương ứng của file này.
- Viết theo mẫu `docs/vi/tro-ly/mau-brief.md`.
```

- [ ] **Step 7: Tạo `docs/vi/tro-ly/hoat-dong-doan.md`**

```markdown
# Loại việc: Hoạt động Đoàn – sự kiện

File dành cho AI. Luôn đọc `docs/vi/tro-ly/quy-trinh-hoi.md` trước file này.

## Khi nào dùng

Thầy cô hoặc cán bộ Đoàn cần slide cho một chương trình, cuộc thi hay sự kiện của Đoàn hoặc của học sinh.

Ví dụ câu lệnh:
- "Làm slide giới thiệu cuộc thi Tuổi trẻ sáng tạo của Đoàn trường"
- "Tạo slide tổng kết trao giải hội thao 26/3"

## Câu hỏi bắt buộc

1. Tên chương trình, dịp tổ chức và cấp tổ chức (chi đoàn, Đoàn trường, hay cấp khác)?
   Gợi ý: lấy từ câu lệnh; cấp tổ chức theo hồ sơ đơn vị.
2. Slide dùng để làm gì: giới thiệu chương trình, phổ biến thể lệ, chiếu trên sân khấu, hay tổng kết và trao giải?
   Gợi ý: giới thiệu chương trình.
3. Thời gian, địa điểm và đối tượng tham gia?
   Gợi ý: đoàn viên, học sinh toàn trường.
4. Nội dung chính gồm những gì: thể lệ, cơ cấu giải, lịch trình, đơn vị phối hợp, nhà tài trợ?
   Gợi ý: thể lệ, lịch trình và cơ cấu giải.
5. Dùng nhận diện nào: huy hiệu Đoàn, logo riêng của chương trình, màu riêng?
   Gợi ý: huy hiệu Đoàn và màu chủ đạo trong hồ sơ.
6. Có ảnh hoạt động sẵn không, hay để AI tạo ảnh minh hoạ?
   Gợi ý: dùng ảnh thầy cô gửi; thiếu thì để AI tạo ảnh minh hoạ.

## Câu hỏi tuỳ chọn

- Có cần trang vinh danh cá nhân hoặc tập thể không? Chỉ hỏi khi mục đích là tổng kết và trao giải.

## Tạo nhanh

1. Tên chương trình (câu hỏi bắt buộc 1).
2. Mục đích của slide (câu hỏi bắt buộc 2).

## Cấu trúc gợi ý

- Giới thiệu chương trình: Trang bìa → Mục đích, ý nghĩa → Đối tượng tham gia → Nội dung chính → Lịch trình → Liên hệ.
- Phổ biến thể lệ: Trang bìa → Điều kiện tham gia → Các vòng thi → Tiêu chí chấm → Giải thưởng → Mốc thời gian.
- Tổng kết và trao giải: Trang bìa → Kết quả nổi bật → Hình ảnh hoạt động → Vinh danh → Cảm ơn.

## Phong cách gợi ý

- Trẻ trung, năng động; chữ đậm, đọc được từ xa.
- Huy hiệu Đoàn giữ đúng tỉ lệ và màu gốc: không kéo giãn, không đổi màu.

## Khổ slide

- Mặc định `ppt169`.

## Ghi vào brief

- Loại việc: Hoạt động Đoàn – sự kiện.
- Thầy cô yêu cầu: tên chương trình, dịp, cấp tổ chức, mục đích slide, thời gian, địa điểm, đối tượng, nội dung chính, nhận diện, nguồn ảnh — ghi đúng lời thầy cô.
- AI đề xuất (thầy cô đã đồng ý): cấu trúc theo mục đích và các gợi ý thầy cô chấp nhận.
- Cấu trúc gợi ý, Phong cách gợi ý, Khổ slide: theo các mục tương ứng của file này.
- Viết theo mẫu `docs/vi/tro-ly/mau-brief.md`.
```

- [ ] **Step 8: Tạo `docs/vi/tro-ly/poster-mang-xa-hoi.md`**

```markdown
# Loại việc: Poster/ấn phẩm Zalo – Facebook

File dành cho AI. Luôn đọc `docs/vi/tro-ly/quy-trinh-hoi.md` trước file này.

## Khi nào dùng

Thầy cô cần một ảnh hoặc chuỗi ảnh để đăng lên Zalo, Facebook hoặc TikTok: thông báo, tuyển sinh, chia sẻ kiến thức, cổ động.

Ví dụ câu lệnh:
- "Làm poster Zalo thông báo họp phụ huynh"
- "Tạo 5 ảnh Facebook chia sẻ mẹo ôn thi"

## Câu hỏi bắt buộc

1. Thầy cô đăng ở kênh nào: nhóm Zalo lớp hoặc phụ huynh, Fanpage Facebook, hay TikTok?
   Gợi ý: nhóm Zalo phụ huynh.
2. Dùng khổ ảnh nào?
   Gợi ý: nhóm Zalo và Facebook dùng ảnh vuông; bài dọc Facebook, TikTok dùng khổ 3:4; story dùng khổ 9:16.
3. Làm một ảnh hay chuỗi nhiều ảnh? Nếu là chuỗi thì bao nhiêu ảnh?
   Gợi ý: một ảnh cho thông báo; 5 ảnh cho chia sẻ kiến thức.
4. Thông điệp chính là gì, và thông tin nào bắt buộc phải có (thời gian, địa điểm, đường link hoặc mã QR, hạn đăng ký)?
   Gợi ý: một câu thông điệp chính cùng thời gian và địa điểm.
5. Người đọc là ai, và thầy cô muốn họ làm gì sau khi xem (đăng ký, tham dự, chia sẻ)?
   Gợi ý: cha mẹ học sinh, đến họp đúng giờ.
6. Có ảnh sẵn không, hay để AI tạo ảnh? Có dùng logo không?
   Gợi ý: để AI tạo ảnh minh hoạ; dùng logo trong hồ sơ.

## Câu hỏi tuỳ chọn

- Có mã QR hoặc đường link cần chèn không? Chỉ hỏi khi có đăng ký hoặc khảo sát.

## Tạo nhanh

1. Kênh đăng (câu hỏi bắt buộc 1).
2. Thông điệp chính và thông tin bắt buộc (câu hỏi bắt buộc 4).

## Cấu trúc gợi ý

- Một ảnh: Tiêu đề lớn → Thông tin chính → Lời kêu gọi hành động → Logo hoặc mã QR.
- Chuỗi ảnh: ảnh đầu gây chú ý; mỗi ảnh giữa một ý; ảnh cuối kêu gọi hành động.

## Phong cách gợi ý

- Chữ lớn, đọc được trên điện thoại; khoảng 30 chữ mỗi ảnh.
- Tương phản cao; tối đa 2 màu chủ đạo cộng 1 màu nhấn.

## Khổ slide

- Ảnh vuông cho nhóm Zalo và Facebook: `moments`.
- Bài dọc 3:4 cho Facebook và TikTok: `xiaohongshu`.
- Story và Reels 9:16: `story`.
- Ảnh bìa bài viết Zalo OA: `wechat`.

## Ghi vào brief

- Loại việc: Poster/ấn phẩm Zalo – Facebook.
- Thầy cô yêu cầu: kênh đăng, khổ ảnh, số ảnh, thông điệp, thông tin bắt buộc, người đọc, hành động mong muốn, nguồn ảnh, logo — ghi đúng lời thầy cô.
- AI đề xuất (thầy cô đã đồng ý): khổ ảnh theo kênh và các gợi ý thầy cô chấp nhận.
- Cấu trúc gợi ý, Phong cách gợi ý, Khổ slide: theo các mục tương ứng của file này.
- Viết theo mẫu `docs/vi/tro-ly/mau-brief.md`.
```

- [ ] **Step 9: Tạo `docs/vi/tro-ly/tap-huan-workshop.md`**

```markdown
# Loại việc: Tập huấn/workshop

File dành cho AI. Luôn đọc `docs/vi/tro-ly/quy-trinh-hoi.md` trước file này.

## Khi nào dùng

Thầy cô tự làm slide cho buổi tập huấn, bồi dưỡng, sinh hoạt chuyên môn hoặc chia sẻ chuyên đề với đồng nghiệp.

Ví dụ câu lệnh:
- "Làm slide tập huấn dùng AI soạn giáo án cho giáo viên"
- "Tạo slide sinh hoạt tổ chuyên môn về đổi mới kiểm tra đánh giá"

## Câu hỏi bắt buộc

1. Chủ đề là gì, và buổi này theo hình thức nào: sinh hoạt tổ chuyên môn, bồi dưỡng thường xuyên, chia sẻ chuyên đề, hay tập huấn toàn trường?
   Gợi ý: lấy chủ đề từ câu lệnh; hình thức sinh hoạt tổ chuyên môn.
2. Ai tham dự, và họ đã biết về chủ đề đến mức nào?
   Gợi ý: giáo viên trong tổ, mới biết sơ qua về chủ đề.
3. Buổi kéo dài bao lâu, và tỉ lệ lý thuyết – thực hành thế nào?
   Gợi ý: 90 phút, một nửa thời gian thực hành.
4. Sau buổi, người học làm được gì (2–3 kết quả cụ thể)?
   Gợi ý: AI đề xuất 3 kết quả đo được để thầy cô sửa.
5. Có phần hướng dẫn thao tác từng bước không? Thầy cô có ảnh chụp màn hình để gửi không?
   Gợi ý: có hướng dẫn từng bước; dùng ảnh chụp màn hình thầy cô gửi.
6. Có những hoạt động tương tác nào: câu hỏi thảo luận, bài tập thực hành, phiếu khảo sát?
   Gợi ý: một bài tập thực hành sau mỗi phần và một câu hỏi thảo luận cuối buổi.

## Câu hỏi tuỳ chọn

- Có cần tài liệu phát tay hoặc đường link tài liệu sau buổi không? Chỉ hỏi khi thầy cô nhắc tới tài liệu kèm theo.

## Tạo nhanh

1. Chủ đề (câu hỏi bắt buộc 1).
2. Đối tượng tham dự (câu hỏi bắt buộc 2).
3. Thời lượng (câu hỏi bắt buộc 3).

## Cấu trúc gợi ý

- Trang bìa → Mục tiêu buổi học → Vì sao cần → Nội dung và thao tác (bài tập ngay sau mỗi phần) → Thực hành → Hỏi đáp → Tổng kết và tài liệu.

## Phong cách gợi ý

- Gần gũi, rõ ràng; mỗi slide thao tác một bước kèm ảnh chụp màn hình.
- Đề xuất mode `instructional` và style `workshop-teaching` của upstream.

## Khổ slide

- Mặc định `ppt169`.

## Ghi vào brief

- Loại việc: Tập huấn/workshop.
- Thầy cô yêu cầu: chủ đề, hình thức, đối tượng, mức độ đã biết, thời lượng, tỉ lệ thực hành, kết quả mong muốn, phần thao tác, hoạt động tương tác — ghi đúng lời thầy cô.
- AI đề xuất (thầy cô đã đồng ý): kết quả sau buổi và các gợi ý thầy cô chấp nhận.
- Cấu trúc gợi ý, Phong cách gợi ý, Khổ slide: theo các mục tương ứng của file này.
- Viết theo mẫu `docs/vi/tro-ly/mau-brief.md`.
```

- [ ] **Step 10: Chạy test, xác nhận đạt**

Run: `& $PY -m unittest discover -s tools/vi/tests -v`
Expected: toàn bộ OK (70 test cũ + 7 test mới = 77).

- [ ] **Step 11: Commit**

```powershell
git add docs/vi/tro-ly/mau-ho-so-don-vi.md docs/vi/tro-ly/mau-brief.md docs/vi/tro-ly/bai-giang.md docs/vi/tro-ly/bao-cao-tong-ket.md docs/vi/tro-ly/hoat-dong-doan.md docs/vi/tro-ly/poster-mang-xa-hoi.md docs/vi/tro-ly/tap-huan-workshop.md tools/vi/tests/test_vi_layer.py
git commit -m "feat(vi): add teacher intake guides for five task types" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```
(Thay dòng `Co-Authored-By` bằng dòng ghi công đúng của phiên đang thực hiện.)

---

### Task 2: Quy trình hỏi chung — `quy-trinh-hoi.md`

**Files:**
- Create: `docs/vi/tro-ly/quy-trinh-hoi.md`
- Test: `tools/vi/tests/test_vi_layer.py`

**Interfaces:**
- Consumes: `h2_headings`, `section`, `read`, `TRO_LY_DIR`, `GUIDE_FILES` (Task 1); 5 file loại việc và 2 file mẫu đã tồn tại (link trong file mới phải trỏ tới chúng).
- Produces: hằng `COMMON_HEADINGS`, class `TeacherAssistantCommonRulesTest`; file `docs/vi/tro-ly/quy-trinh-hoi.md` mà Task 3 link tới.

- [ ] **Step 1: Viết test thất bại**

Thêm trước `if __name__ == "__main__":`:

```python
COMMON_HEADINGS = (
    "## Khi nào áp dụng",
    "## Thứ tự ưu tiên",
    "## Hồ sơ đơn vị",
    "## Cách hỏi",
    "## Ghi brief và đưa vào dự án",
    "## Tạo nhanh",
    "## Đổi ý giữa chừng",
)


class TeacherAssistantCommonRulesTest(unittest.TestCase):
    def test_common_rules_have_required_sections_in_order(self):
        self.assertEqual(h2_headings(read("docs/vi/tro-ly/quy-trinh-hoi.md")), list(COMMON_HEADINGS))

    def test_common_rules_state_key_constraints(self):
        text = read("docs/vi/tro-ly/quy-trinh-hoi.md")
        for phrase in (
            "projects/_ho-so-don-vi.md",
            "projects/_brief-",
            "import-sources",
            "--copy",
            "7 câu",
            "(AI đề xuất)",
            "SKILL.md",
            "confirm-surface.md",
            "quick-generate.md",
        ):
            self.assertIn(phrase, text)

    def test_common_rules_link_every_guide_and_template(self):
        links = LINK_RE.findall(section(read("docs/vi/tro-ly/quy-trinh-hoi.md"), "## Khi nào áp dụng"))
        for name in GUIDE_FILES:
            self.assertIn(name, links)
        text = read("docs/vi/tro-ly/quy-trinh-hoi.md")
        for name in ("mau-ho-so-don-vi.md", "mau-brief.md"):
            self.assertIn(f"]({name})", text)
```

- [ ] **Step 2: Chạy test, xác nhận thất bại**

Run: `& $PY -m unittest discover -s tools/vi/tests -p "test_vi_layer.py" -v`
Expected: 3 test `TeacherAssistantCommonRulesTest` ERROR (`FileNotFoundError`).

- [ ] **Step 3: Tạo `docs/vi/tro-ly/quy-trinh-hoi.md`**

````markdown
# Quy trình hỏi thầy cô trước khi tạo PPTX

File dành cho AI. Nó bổ sung ngữ cảnh cho quy trình tạo PPTX trong `skills/ppt-master/SKILL.md` và không thay thế bước nào của quy trình đó.

## Khi nào áp dụng

Áp dụng khi cả hai điều kiện đều đúng:

1. Người dùng viết tiếng Việt.
2. Yêu cầu thuộc một trong 5 loại việc dưới đây.

| Loại việc | Dấu hiệu nhận biết | File hướng dẫn |
|---|---|---|
| Bài giảng | "bài giảng", "giáo án", "tiết học", "bài dạy", tên môn kèm lớp | [bai-giang.md](bai-giang.md) |
| Báo cáo – tổng kết | "báo cáo", "sơ kết", "tổng kết", "thi đua", "hội nghị viên chức" | [bao-cao-tong-ket.md](bao-cao-tong-ket.md) |
| Hoạt động Đoàn – sự kiện | "Đoàn", "chi đoàn", "cuộc thi", "sự kiện", "lễ kỷ niệm", "trao giải" | [hoat-dong-doan.md](hoat-dong-doan.md) |
| Poster/ấn phẩm Zalo – Facebook | "poster", "ảnh đăng Zalo", "bài đăng Facebook", "story", "TikTok" | [poster-mang-xa-hoi.md](poster-mang-xa-hoi.md) |
| Tập huấn/workshop | "tập huấn", "bồi dưỡng", "sinh hoạt chuyên môn", "workshop", "chia sẻ chuyên đề" | [tap-huan-workshop.md](tap-huan-workshop.md) |

- Yêu cầu không thuộc loại nào: không dùng file này, làm theo `SKILL.md` như bình thường.
- Không rõ thuộc loại nào, hoặc thuộc hai loại: hỏi đúng một câu để thầy cô chọn loại, kèm gợi ý loại gần nhất.
- Người dùng viết tiếng Anh hoặc ngôn ngữ khác: không dùng bộ câu hỏi này.

## Thứ tự ưu tiên

- `skills/ppt-master/SKILL.md` và `AGENTS.md` luôn được ưu tiên khi có mâu thuẫn với file này.
- Lượt hỏi ở đây diễn ra trước quy trình của upstream và chỉ tạo thêm tài liệu nguồn. Bước xác nhận của upstream vẫn bắt buộc, trừ khi người dùng yêu cầu tạo nhanh.
- Không sửa, bỏ qua hay tìm cách "sửa chữa" `skills/ppt-master/scripts/attribution_guard.py`.

## Hồ sơ đơn vị

- Hồ sơ lưu ở `projects/_ho-so-don-vi.md`, theo mẫu [mau-ho-so-don-vi.md](mau-ho-so-don-vi.md). Thư mục `projects/` không được đưa lên GitHub.
- Chưa có hồ sơ, hoặc hồ sơ thiếu mục: hỏi các mục còn thiếu trong cùng tin nhắn với câu hỏi của loại việc.
- Đã có hồ sơ: mở đầu lượt hỏi bằng dòng "Hồ sơ hiện tại: <Đơn vị> — <Người trình bày>. Dùng tiếp?". Máy có thể có nhiều người dùng chung, nên luôn có dòng này.
- Thầy cô muốn đổi thông tin (ví dụ "đổi màu", "đổi trường"): sửa hồ sơ, ghi ngày hôm nay vào mục "Cập nhật", báo lại một dòng tóm tắt.
- Không bao giờ import file hồ sơ vào dự án. Chỉ chép các mục cần dùng sang brief.
- Đường dẫn logo không tồn tại: báo cho thầy cô, xin đường dẫn khác hoặc làm tiếp không dùng logo.

## Cách hỏi

- Gửi một tin nhắn duy nhất, rồi dừng và chờ thầy cô trả lời. Trong lúc chờ không khởi tạo dự án, không tra cứu, không soạn slide.
- Hỏi tối đa 7 câu bắt buộc của loại việc. Phần hồ sơ đơn vị ở lần đầu không tính vào 7 câu này.
- Đánh số từng câu. Mỗi câu kèm gợi ý cụ thể để thầy cô chỉ cần trả lời "đồng ý" hoặc sửa.
- Chỉ hỏi thông tin mà chỉ thầy cô biết. Không hỏi kiến thức của bài học hay dữ kiện AI tự tra cứu được.
- Câu hỏi tuỳ chọn chỉ thêm khi yêu cầu có liên quan, và tổng số câu vẫn không quá 7.
- Kết thúc tin nhắn bằng dòng chốt cách xác nhận, theo mục "Cách xác nhận" trong hồ sơ (chưa có thì mặc định khung chat):
  - Khung chat: "Ở bước xác nhận, em sẽ tóm tắt trong khung chat để thầy cô duyệt, không mở trang web. Thầy cô đồng ý nhé?"
  - Trang web: "Ở bước xác nhận, em sẽ mở trang web xác nhận để thầy cô duyệt. Thầy cô đồng ý nhé?"
- Thầy cô đồng ý xác nhận trong khung chat ngay trong lần chạy này là chỉ dẫn hợp lệ để dùng nhánh khung chat của `skills/ppt-master/references/confirm-surface.md` (mục 1).
- Ở bước xác nhận, nếu thầy cô nói "AI tự quyết" hoặc giao hẳn cho AI: dùng nhánh ủy quyền trong mục 1 của `skills/ppt-master/references/confirm-surface.md` (AI chốt đề xuất và đưa một bản tóm tắt cuối).
- Thầy cô muốn dùng trang web xác nhận: dùng nhánh trang web mặc định của upstream; brief vẫn được dùng để điền sẵn đề xuất.
- Thầy cô trả lời thiếu: dùng gợi ý và ghi kèm "(AI đề xuất)" trong brief. Chỉ hỏi lại câu bắt buộc không thể đoán (ví dụ tên bài, loại báo cáo), và hỏi lại tối đa một lần.

## Ghi brief và đưa vào dự án

1. Tạo hoặc cập nhật `projects/_ho-so-don-vi.md`.
2. Viết `projects/_brief-<tên_dự_án>.md` theo mẫu [mau-brief.md](mau-brief.md); `<tên_dự_án>` là tên sẽ dùng khi khởi tạo dự án.
   - Mục "Thầy cô yêu cầu": ghi đúng lời thầy cô, mỗi ý một dòng. Điều cấm ghi nguyên văn.
   - Mục "AI đề xuất (thầy cô đã đồng ý)": các gợi ý thầy cô chấp nhận và các mục ghi "(AI đề xuất)".
   - Mục "Cấu trúc gợi ý" là điểm khởi đầu, không phải bố cục cuối, trừ khi thầy cô yêu cầu giữ nguyên.
3. Làm theo `SKILL.md`. Sau khi khởi tạo dự án, import brief bằng một lệnh riêng, không thêm `--copy`, để file được chuyển vào `sources/` của dự án:

   ```
   python skills/ppt-master/scripts/project_manager.py import-sources <đường_dẫn_dự_án> projects/_brief-<tên_dự_án>.md
   ```

4. Nếu có logo, import bằng một lệnh riêng khác. Chỉ thêm `--copy` khi file logo nằm trong thư mục `projects/`:

   ```
   python skills/ppt-master/scripts/project_manager.py import-sources <đường_dẫn_dự_án> <đường_dẫn_logo>
   ```

5. Ở bước xác nhận của upstream, dùng brief để điền sẵn đề xuất. Nếu thầy cô đã đồng ý xác nhận trong khung chat, trình bày và chờ duyệt trong khung chat.

## Tạo nhanh

- Áp dụng khi câu lệnh có "tạo nhanh", "làm nhanh" hoặc "không cần hỏi lại".
- Chỉ hỏi các câu trong mục "Tạo nhanh" của file loại việc, trong một tin nhắn. Nếu câu lệnh đã có đủ các thông tin đó thì không hỏi.
- Viết brief như mục trên; các mục không hỏi ghi "(AI đề xuất)".
- Chạy chế độ tạo nhanh của upstream theo `skills/ppt-master/workflows/profiles/quick-generate.md`, không có bước xác nhận.

## Đổi ý giữa chừng

- Thầy cô đổi thông tin trước bước xác nhận: cập nhật brief. Nếu brief đã được import, cập nhật file brief trong `sources/` của dự án.
- Đã qua bước xác nhận: nêu rõ thay đổi để thầy cô duyệt lại ở bước xác nhận của upstream; không tự áp dụng thay đổi khi thầy cô chưa duyệt.
````

- [ ] **Step 4: Chạy test, xác nhận đạt**

Run: `& $PY -m unittest discover -s tools/vi/tests -v`
Expected: toàn bộ OK (80 test), gồm `test_relative_markdown_links_resolve` (link trong `quy-trinh-hoi.md` phải trỏ tới file có thật).

- [ ] **Step 5: Commit**

```powershell
git add docs/vi/tro-ly/quy-trinh-hoi.md tools/vi/tests/test_vi_layer.py
git commit -m "feat(vi): add shared teacher intake procedure" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```
(Thay dòng `Co-Authored-By` bằng dòng ghi công đúng của phiên đang thực hiện.)

---

### Task 3: Điểm vào cho AI — `AGENTS.vi.md` mục 10

**Files:**
- Modify: `AGENTS.vi.md` (thêm mục mới ở cuối file, sau mục 9)
- Test: `tools/vi/tests/test_vi_layer.py`

**Interfaces:**
- Consumes: `h2_headings`, `section`, `read`, `GUIDE_FILES` (Task 1); `docs/vi/tro-ly/quy-trinh-hoi.md` (Task 2).
- Produces: class `TeacherAssistantWiringTest`; tiêu đề `## 10. Hỗ trợ thầy cô trước khi tạo PPTX` mà Task 5 kiểm hành vi.

- [ ] **Step 1: Viết test thất bại**

Thêm trước `if __name__ == "__main__":`:

```python
AGENTS_VI_ASSISTANT_HEADING = "## 10. Hỗ trợ thầy cô trước khi tạo PPTX"


class TeacherAssistantWiringTest(unittest.TestCase):
    def test_agents_vi_has_assistant_section_last(self):
        headings = h2_headings(read("AGENTS.vi.md"))
        self.assertEqual(headings[-1], AGENTS_VI_ASSISTANT_HEADING)

    def test_assistant_section_links_common_rules_and_all_guides(self):
        body = section(read("AGENTS.vi.md"), AGENTS_VI_ASSISTANT_HEADING)
        self.assertIn("(docs/vi/tro-ly/quy-trinh-hoi.md)", body)
        for name in GUIDE_FILES:
            self.assertIn(f"(docs/vi/tro-ly/{name})", body)

    def test_assistant_section_keeps_upstream_priority_and_quick_mode(self):
        body = section(read("AGENTS.vi.md"), AGENTS_VI_ASSISTANT_HEADING)
        for phrase in ("SKILL.md", "bước xác nhận", "tạo nhanh"):
            self.assertIn(phrase, body)
```

- [ ] **Step 2: Chạy test, xác nhận thất bại**

Run: `& $PY -m unittest discover -s tools/vi/tests -p "test_vi_layer.py" -v`
Expected: 3 test `TeacherAssistantWiringTest` FAIL/ERROR ("Thiếu mục" hoặc tiêu đề cuối khác).

- [ ] **Step 3: Thêm mục 10 vào cuối `AGENTS.vi.md`**

Thêm sau mục 9 (giữ một dòng trống giữa hai mục), không sửa các mục 1–9:

```markdown
## 10. Hỗ trợ thầy cô trước khi tạo PPTX

Khi người dùng viết tiếng Việt và yêu cầu thuộc một trong 5 loại việc dưới đây, đọc [docs/vi/tro-ly/quy-trinh-hoi.md](docs/vi/tro-ly/quy-trinh-hoi.md) trước, rồi đọc file của loại việc đó. Hỏi thầy cô một lượt và chờ trả lời trước khi khởi tạo dự án.

| Loại việc | File hướng dẫn |
|---|---|
| Bài giảng | [docs/vi/tro-ly/bai-giang.md](docs/vi/tro-ly/bai-giang.md) |
| Báo cáo – tổng kết | [docs/vi/tro-ly/bao-cao-tong-ket.md](docs/vi/tro-ly/bao-cao-tong-ket.md) |
| Hoạt động Đoàn – sự kiện | [docs/vi/tro-ly/hoat-dong-doan.md](docs/vi/tro-ly/hoat-dong-doan.md) |
| Poster/ấn phẩm Zalo – Facebook | [docs/vi/tro-ly/poster-mang-xa-hoi.md](docs/vi/tro-ly/poster-mang-xa-hoi.md) |
| Tập huấn/workshop | [docs/vi/tro-ly/tap-huan-workshop.md](docs/vi/tro-ly/tap-huan-workshop.md) |

- `SKILL.md` vẫn được ưu tiên. Lượt hỏi này chỉ tạo thêm tài liệu nguồn; bước xác nhận của upstream vẫn bắt buộc, trừ khi người dùng yêu cầu tạo nhanh (xem mục 3).
- Yêu cầu không thuộc 5 loại, hoặc người dùng không viết tiếng Việt: làm theo `SKILL.md` như bình thường.
```

- [ ] **Step 4: Chạy test, xác nhận đạt**

Run: `& $PY -m unittest discover -s tools/vi/tests -v`
Expected: toàn bộ OK (83 test), gồm `test_format_map_uses_existing_canvas_keys` (bảng khổ canvas mục 5 không đổi) và link test.

- [ ] **Step 5: Commit**

```powershell
git add AGENTS.vi.md tools/vi/tests/test_vi_layer.py
git commit -m "feat(vi): route Vietnamese teacher requests through the intake guides" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```
(Thay dòng `Co-Authored-By` bằng dòng ghi công đúng của phiên đang thực hiện.)

---

### Task 4: Tài liệu cho người dùng

**Files:**
- Modify: `docs/vi/bat-dau-nhanh.md` (thay nội dung mục `## AI sẽ hỏi gì`)
- Modify: `docs/vi/cau-lenh-mau.md` (thêm mục `## Trả lời lượt hỏi của AI` ngay sau mục `## Bài giảng`)
- Modify: `README.md` (thêm một gạch đầu dòng vào mục `## Làm được gì`)
- Test: `tools/vi/tests/test_vi_layer.py`

**Interfaces:**
- Consumes: `section`, `h2_headings`, `read` (Task 1).
- Produces: class `TeacherAssistantUserDocsTest`.

- [ ] **Step 1: Viết test thất bại**

Thêm trước `if __name__ == "__main__":`:

```python
class TeacherAssistantUserDocsTest(unittest.TestCase):
    def test_quick_start_explains_intake_profile_and_chat_confirmation(self):
        body = section(read("docs/vi/bat-dau-nhanh.md"), "## AI sẽ hỏi gì")
        for phrase in ("hồ sơ đơn vị", "tạo nhanh", "khung chat", "7 câu"):
            self.assertIn(phrase, body)

    def test_sample_commands_show_how_to_answer_intake(self):
        headings = h2_headings(read("docs/vi/cau-lenh-mau.md"))
        self.assertIn("## Trả lời lượt hỏi của AI", headings)
        self.assertEqual(headings.index("## Trả lời lượt hỏi của AI"), headings.index("## Bài giảng") + 1)

    def test_readme_mentions_teacher_intake(self):
        self.assertIn("Hỏi thầy cô một lượt ngắn", section(read("README.md"), "## Làm được gì"))
```

- [ ] **Step 2: Chạy test, xác nhận thất bại**

Run: `& $PY -m unittest discover -s tools/vi/tests -p "test_vi_layer.py" -v`
Expected: 3 test `TeacherAssistantUserDocsTest` FAIL.

- [ ] **Step 3: Thay nội dung mục `## AI sẽ hỏi gì` trong `docs/vi/bat-dau-nhanh.md`**

Giữ nguyên dòng tiêu đề `## AI sẽ hỏi gì`; thay toàn bộ nội dung bên dưới nó (đến trước `## Lấy file kết quả`) bằng:

```markdown
Với bài giảng, báo cáo – tổng kết, hoạt động Đoàn, poster Zalo – Facebook và tập huấn, AI hỏi một lượt ngắn trước khi làm: tối đa 7 câu, mỗi câu có sẵn gợi ý. Thầy cô chỉ cần trả lời "đồng ý" hoặc sửa những ý chưa đúng.

- **Lần đầu**, AI hỏi thêm hồ sơ đơn vị: tên trường, cấp học, người trình bày, logo, màu chủ đạo. Hồ sơ lưu ở `projects\_ho-so-don-vi.md` trên máy của thầy cô, lần sau AI dùng lại.
- **Sau đó**, AI tóm tắt đề xuất trong khung chat để thầy cô duyệt lần cuối, rồi mới dựng slide.
- **Việc khác** (ví dụ giới thiệu sản phẩm), AI xác nhận lại đối tượng người xem, số trang, phong cách và mẫu thiết kế như trước.

Muốn AI làm luôn, thêm chữ "tạo nhanh" vào câu lệnh: AI chỉ hỏi 2–3 câu thật cần thiết.
```

- [ ] **Step 4: Thêm mục mới vào `docs/vi/cau-lenh-mau.md`**

Chèn ngay sau khối code của mục `## Bài giảng` (trước `## Báo cáo tổng kết`):

````markdown
## Trả lời lượt hỏi của AI

AI hỏi các câu đánh số, mỗi câu có gợi ý. Thầy cô trả lời theo số thứ tự, câu nào đồng ý thì chỉ cần ghi "đồng ý":

```
1. Đồng ý
2. Kết nối tri thức với cuộc sống
3. 2 tiết
4. Đồng ý gợi ý
5. Thêm trò chơi ô chữ khởi động
6. Đồng ý
7. Không có
Xác nhận trong khung chat.
```
````

- [ ] **Step 5: Thêm gạch đầu dòng vào mục `## Làm được gì` của `README.md`**

Thêm làm gạch đầu dòng đầu tiên của mục (các dòng khác giữ nguyên):

```markdown
- Hỏi thầy cô một lượt ngắn (môn, lớp, bộ sách, mục tiêu, đơn vị…) trước khi làm bài giảng, báo cáo, hoạt động Đoàn, poster và tập huấn, nên nội dung sát thực tế.
```

- [ ] **Step 6: Chạy test, xác nhận đạt**

Run: `& $PY -m unittest discover -s tools/vi/tests -v`
Expected: toàn bộ OK (86 test), gồm `test_readme_credits_upstream` và link test.

- [ ] **Step 7: Commit**

```powershell
git add docs/vi/bat-dau-nhanh.md docs/vi/cau-lenh-mau.md README.md tools/vi/tests/test_vi_layer.py
git commit -m "docs(vi): explain the teacher intake round in user guides" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```
(Thay dòng `Co-Authored-By` bằng dòng ghi công đúng của phiên đang thực hiện.)

---

### Task 5: Kiểm tra hành vi bằng Claude Code headless

**Files:**
- Create: `docs/vi/phat-trien/2026-09-11-m2-kiem-thu-headless.md`
- Modify (chỉ khi một ca không đạt): `AGENTS.vi.md` mục 10, `docs/vi/tro-ly/quy-trinh-hoi.md`, hoặc file loại việc liên quan

**Interfaces:**
- Consumes: toàn bộ sản phẩm Task 1–4; `claude` CLI (đã xác minh phiên bản 2.1.212 có `-p` và `--disallowedTools <tools...>`).
- Produces: báo cáo kiểm thử 7 ca (tiêu chí thành công #1–#3 của spec).

- [ ] **Step 1: Chạy 7 ca**

Chạy bằng Bash tool từ thư mục gốc repo (Git Bash truyền tiếng Việt đúng). Câu lệnh đứng ngay sau `-p`, cờ `--disallowedTools` đứng cuối. Mỗi ca lưu đầu ra vào thư mục tạm:

```bash
cd "/c/Users/ADMIN/Downloads/VIBE CODING/PPTmaster"
OUT="/c/Users/ADMIN/AppData/Local/Temp/claude/c--Users-ADMIN-Downloads-VIBE-CODING-PPTmaster/c49c4a0d-7ba3-41e7-84e8-5f1e4b4ad88f/scratchpad/m2-headless"
mkdir -p "$OUT"
run_case() { timeout 300 claude -p "$2" --disallowedTools Bash Write Edit NotebookEdit > "$OUT/case$1.txt" 2>&1; echo "case$1 exit=$?"; }
run_case 1 "Tạo bài giảng Vật lí 10 bài Chuyển động thẳng đều"
run_case 2 "Làm slide báo cáo tổng kết năm học của tổ Toán"
run_case 3 "Làm slide giới thiệu cuộc thi Tuổi trẻ sáng tạo của Đoàn trường"
run_case 4 "Làm poster Zalo thông báo họp phụ huynh"
run_case 5 "Làm slide tập huấn dùng AI soạn giáo án cho giáo viên"
run_case 6 "Tạo nhanh bài giảng Hoá học 11"
run_case 7 "Make a 5-slide deck about our software product"
```
Expected: 7 dòng `caseN exit=0`.

- [ ] **Step 2: Chấm từng ca**

```bash
OUT="/c/Users/ADMIN/AppData/Local/Temp/claude/c--Users-ADMIN-Downloads-VIBE-CODING-PPTmaster/c49c4a0d-7ba3-41e7-84e8-5f1e4b4ad88f/scratchpad/m2-headless"
has() { grep -qi -- "$2" "$OUT/case$1.txt"; }
qcount() { grep -cE '^\s*[0-9]+[.)] ' "$OUT/case$1.txt"; }
check() { if eval "$2"; then echo "case$1 PASS"; else echo "case$1 FAIL"; fi; }
check 1 'has 1 "bộ sách" && has 1 "tiết" && has 1 "mục tiêu" && ! has 1 "đã khởi tạo"'
check 2 'has 2 "kỳ báo cáo" && has 2 "người nghe" && has 2 "số liệu" && ! has 2 "đã khởi tạo"'
check 3 'has 3 "mục đích" && has 3 "thời gian" && (has 3 "thể lệ" || has 3 "giải") && ! has 3 "đã khởi tạo"'
check 4 'has 4 "kênh" && (has 4 "khổ" || has 4 "vuông") && has 4 "thông điệp" && ! has 4 "đã khởi tạo"'
check 5 'has 5 "đối tượng" && has 5 "thời lượng" && (has 5 "làm được" || has 5 "kết quả") && ! has 5 "đã khởi tạo"'
check 6 'has 6 "bộ sách" && [ "$(qcount 6)" -ge 2 ] && [ "$(qcount 6)" -le 3 ]'
check 7 '! has 7 "Gợi ý:" && ! has 7 "bộ sách" && ! has 7 "hồ sơ đơn vị"'
for n in 1 2 3 4 5 6 7; do echo "--- case$n (20 dòng đầu)"; head -20 "$OUT/case$n.txt"; done
```
Expected: 7 dòng `PASS`.

- [ ] **Step 3: Xử lý ca không đạt (nếu có)**

Với mỗi ca `FAIL`: đọc toàn bộ `case<N>.txt`, xác định AI bỏ qua quy tắc nào, sửa **tối thiểu** câu chữ ở nơi quy tắc đó được viết (`AGENTS.vi.md` mục 10, `docs/vi/tro-ly/quy-trinh-hoi.md` hoặc file loại việc) để chỉ dẫn rõ hơn, không đổi cấu trúc mục. Chạy lại `& $PY -m unittest discover -s tools/vi/tests -v` (phải OK), rồi chạy lại **cả 7 ca** (Step 1–2). Tối đa 2 vòng sửa; nếu vẫn còn ca FAIL, dừng và báo cáo DONE_WITH_CONCERNS kèm đầu ra của ca đó.

- [ ] **Step 4: Ghi báo cáo kiểm thử**

Tạo `docs/vi/phat-trien/2026-09-11-m2-kiem-thu-headless.md` với nội dung (điền kết quả thật của lần chạy cuối):

```markdown
# Kiểm tra hành vi trợ lý hỏi đáp (M2) — Claude Code headless

- Ngày chạy: <ngày chạy thật, YYYY-MM-DD>
- Commit được kiểm: <short SHA của HEAD khi chạy>
- Lệnh: `claude -p "câu lệnh của ca" --disallowedTools Bash Write Edit NotebookEdit`
- Số vòng sửa hướng dẫn: <0, 1 hoặc 2>

| Ca | Câu lệnh | Tiêu chí | Kết quả |
|---|---|---|---|
| 1 | Tạo bài giảng Vật lí 10 bài Chuyển động thẳng đều | Hỏi bộ sách, số tiết, mục tiêu; chưa khởi tạo dự án | <PASS/FAIL> |
| 2 | Làm slide báo cáo tổng kết năm học của tổ Toán | Hỏi kỳ báo cáo, người nghe, số liệu | <PASS/FAIL> |
| 3 | Làm slide giới thiệu cuộc thi Tuổi trẻ sáng tạo của Đoàn trường | Hỏi mục đích, thời gian, thể lệ hoặc giải | <PASS/FAIL> |
| 4 | Làm poster Zalo thông báo họp phụ huynh | Hỏi kênh, khổ ảnh, thông điệp | <PASS/FAIL> |
| 5 | Làm slide tập huấn dùng AI soạn giáo án cho giáo viên | Hỏi đối tượng, thời lượng, kết quả sau buổi | <PASS/FAIL> |
| 6 | Tạo nhanh bài giảng Hoá học 11 | 2–3 câu hỏi, có hỏi bộ sách | <PASS/FAIL> |
| 7 | Make a 5-slide deck about our software product | Không dùng bộ câu hỏi Việt | <PASS/FAIL> |

## Trích đoạn đầu ra

<với mỗi ca: 10–20 dòng đầu của câu trả lời, trong khối code>

## Sửa đổi hướng dẫn trong quá trình kiểm thử

<"Không có", hoặc liệt kê file, câu chữ đã sửa và ca liên quan>
```

Trong file thật, thay mọi phần trong ngoặc nhọn bằng dữ liệu thật; không để lại ngoặc nhọn.

- [ ] **Step 5: Kiểm tra lại và commit**

```powershell
$PY = 'C:\Users\ADMIN\AppData\Local\Temp\claude\c--Users-ADMIN-Downloads-VIBE-CODING-PPTmaster\c49c4a0d-7ba3-41e7-84e8-5f1e4b4ad88f\scratchpad\venv312\Scripts\python.exe'
& $PY -m unittest discover -s tools/vi/tests -v
Select-String -Path docs/vi/phat-trien/2026-09-11-m2-kiem-thu-headless.md -Pattern '<[^>]+>' | Measure-Object | Select-Object -ExpandProperty Count
git add docs/vi/phat-trien/2026-09-11-m2-kiem-thu-headless.md AGENTS.vi.md docs/vi/tro-ly
git commit -m "test(vi): record headless behaviour checks for teacher intake" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```
Expected: test OK; số ngoặc nhọn còn sót = 0. (Thay dòng `Co-Authored-By` bằng dòng ghi công đúng của phiên đang thực hiện.)

---

### Task 6: Nghiệm thu của chủ repo (điểm dừng)

**Files:** không có.

**Interfaces:**
- Consumes: nhánh `feat/vi-tro-ly` sau Task 5 và review cuối nhánh.
- Produces: xác nhận của chủ repo cho tiêu chí thành công #4, #5, #7 của spec.

- [ ] **Step 1: Kiểm tra toàn nhánh trước khi nhờ nghiệm thu**

```powershell
$PY = 'C:\Users\ADMIN\AppData\Local\Temp\claude\c--Users-ADMIN-Downloads-VIBE-CODING-PPTmaster\c49c4a0d-7ba3-41e7-84e8-5f1e4b4ad88f\scratchpad\venv312\Scripts\python.exe'
& $PY -m unittest discover -s tools/vi/tests -v
& $PY skills/ppt-master/scripts/attribution_guard.py; "guard=$LASTEXITCODE"
git diff --stat v6.3.2 HEAD -- skills/; "(skills diff end)"
git status --porcelain; "(status end)"
```
Expected: test OK; `guard=0`; diff `skills/` rỗng; status rỗng.

- [ ] **Step 2: Gửi chủ repo danh sách nghiệm thu và chờ trả lời**

Nhờ chủ repo (sẽ mở Antigravity và PowerPoint — báo trước):
1. Mở thư mục repo trên nhánh `feat/vi-tro-ly` trong Antigravity.
2. Nhắn "Tạo bài giảng <môn, lớp, bài thật thầy cô đang dạy>" → AI hỏi một lượt (có phần hồ sơ đơn vị, dòng chốt xác nhận trong khung chat).
3. Trả lời lượt hỏi → AI tóm tắt đề xuất trong khung chat (không mở trang web) → nhắn "đồng ý" → nhận PPTX, mở bằng PowerPoint, chữ tiếng Việt đúng dấu, nội dung đúng bài và bộ sách.
4. Tạo một yêu cầu thứ hai bất kỳ trong 5 loại → AI nêu "Hồ sơ hiện tại: …" và không hỏi lại các mục hồ sơ.

Không sang Task 7 khi chưa có xác nhận "đạt" của chủ repo. Nếu không đạt: ghi lại mô tả lỗi, sửa tối thiểu ở file hướng dẫn liên quan qua một vòng sửa có review, chạy lại Task 5 Step 1–2 và Step 1 của task này, rồi nhờ nghiệm thu lại.

---

### Task 7: Phát hành `v6.3.2-vi.2`

**Files:**
- Modify: `CHANGELOG-VI.md` (thêm mục mới ở đầu, ngay dưới tiêu đề `# Nhật ký thay đổi — Bản Việt`)
- Modify: `README.md` (dòng `Phiên bản:`)

**Interfaces:**
- Consumes: xác nhận "đạt" của chủ repo (Task 6); `gh` đã đăng nhập tài khoản `luonghaianh1208`.
- Produces: `origin/main` chứa M2, tag `v6.3.2-vi.2`, GitHub Release `v6.3.2-vi.2`.

- [ ] **Step 1: Cập nhật CHANGELOG và dòng phiên bản**

Lấy ngày phát hành: `Get-Date -Format yyyy-MM-dd`. Thêm vào `CHANGELOG-VI.md` ngay dưới dòng tiêu đề `# Nhật ký thay đổi — Bản Việt` (trước `## 6.3.2-vi.1 — 2026-09-11`), thay `NGAY_PHAT_HANH` bằng ngày vừa lấy:

```markdown
## 6.3.2-vi.2 — NGAY_PHAT_HANH

Thêm trợ lý hỏi đáp cho thầy cô: AI hỏi một lượt ngắn trước khi tạo slide.

### Thêm
- Bộ hướng dẫn cho AI trong `docs/vi/tro-ly/`: quy trình hỏi chung và 5 loại việc (bài giảng, báo cáo – tổng kết, hoạt động Đoàn – sự kiện, poster Zalo – Facebook, tập huấn/workshop).
- Hồ sơ đơn vị lưu một lần ở `projects/_ho-so-don-vi.md` (không đưa lên GitHub); câu trả lời của thầy cô được ghi thành brief trong thư mục nguồn của dự án.
- Chế độ "tạo nhanh" chỉ hỏi 2–3 câu thật cần thiết.
- `AGENTS.vi.md` mục 10 trỏ AI tới bộ hướng dẫn; tài liệu người dùng giải thích lượt hỏi.

### Không thay đổi
- Lõi PPT Master v6.3.2 của Hugo He giữ nguyên; bước xác nhận của dự án gốc vẫn áp dụng.
```

Trong `README.md`, sửa dòng `Phiên bản: **6.3.2-vi.1** · [Nhật ký thay đổi](CHANGELOG-VI.md)` thành `Phiên bản: **6.3.2-vi.2** · [Nhật ký thay đổi](CHANGELOG-VI.md)`.

- [ ] **Step 2: Chạy test và commit**

```powershell
$PY = 'C:\Users\ADMIN\AppData\Local\Temp\claude\c--Users-ADMIN-Downloads-VIBE-CODING-PPTmaster\c49c4a0d-7ba3-41e7-84e8-5f1e4b4ad88f\scratchpad\venv312\Scripts\python.exe'
& $PY -m unittest discover -s tools/vi/tests -v
git add CHANGELOG-VI.md README.md
git commit -m "docs(vi): release 6.3.2-vi.2" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```
Expected: test OK. (Thay dòng `Co-Authored-By` bằng dòng ghi công đúng của phiên đang thực hiện.)

- [ ] **Step 3: Đưa vào `main`, gắn tag, push**

```powershell
git switch main
git merge --ff-only feat/vi-tro-ly
git tag -a v6.3.2-vi.2 -m "PPT Master ban Viet 6.3.2-vi.2"
git push origin main
git push origin v6.3.2-vi.2
git ls-remote origin refs/heads/main refs/tags/v6.3.2-vi.2
"local main=" + (git rev-parse main)
```
Expected: fast-forward thành công (nếu không phải fast-forward: dừng, báo BLOCKED); push thường thành công (không dùng `--force`, `--tags`); `ls-remote` in đủ 2 ref, `main` từ xa trùng `main` trên máy.

- [ ] **Step 4: Tạo GitHub Release**

```powershell
$PY = 'C:\Users\ADMIN\AppData\Local\Temp\claude\c--Users-ADMIN-Downloads-VIBE-CODING-PPTmaster\c49c4a0d-7ba3-41e7-84e8-5f1e4b4ad88f\scratchpad\venv312\Scripts\python.exe'
$notes = 'C:\Users\ADMIN\AppData\Local\Temp\claude\c--Users-ADMIN-Downloads-VIBE-CODING-PPTmaster\c49c4a0d-7ba3-41e7-84e8-5f1e4b4ad88f\scratchpad\release-notes-vi2.md'
& $PY -c "import re, pathlib, sys; t = pathlib.Path('CHANGELOG-VI.md').read_text(encoding='utf-8'); m = re.search(r'^## 6\.3\.2-vi\.2.*?(?=^## |\Z)', t, re.S | re.M); pathlib.Path(sys.argv[1]).write_text(m.group(0), encoding='utf-8')" $notes
gh release create v6.3.2-vi.2 --repo luonghaianh1208/PPTmaster --title "PPT Master bản Việt 6.3.2-vi.2" --notes-file $notes
gh release view v6.3.2-vi.2 --repo luonghaianh1208/PPTmaster --json url,tagName,isDraft
gh release view v6.3.2-vi.2 --repo hugohe3/ppt-master --json url
```
Expected: in URL release, `tagName` = `v6.3.2-vi.2`, `isDraft` = false; lệnh cuối báo `release not found` (không có gì bị tạo trên repo gốc).

- [ ] **Step 5: Xác minh sau phát hành**

```powershell
$PY = 'C:\Users\ADMIN\AppData\Local\Temp\claude\c--Users-ADMIN-Downloads-VIBE-CODING-PPTmaster\c49c4a0d-7ba3-41e7-84e8-5f1e4b4ad88f\scratchpad\venv312\Scripts\python.exe'
$fresh = 'C:\Users\ADMIN\AppData\Local\Temp\claude\c--Users-ADMIN-Downloads-VIBE-CODING-PPTmaster\c49c4a0d-7ba3-41e7-84e8-5f1e4b4ad88f\scratchpad\freshclone-vi2'
git clone --depth 1 https://github.com/luonghaianh1208/PPTmaster.git $fresh
Push-Location $fresh
& $PY skills/ppt-master/scripts/attribution_guard.py; "guard=$LASTEXITCODE"
& $PY -m unittest discover -s tools/vi/tests
& $PY tools/vi/doctor.py; "doctor=$LASTEXITCODE"
Test-Path docs/vi/tro-ly/quy-trinh-hoi.md
Pop-Location
git status --porcelain; "(status end)"
```
Expected: `guard=0`; test OK (test ranh giới upstream được bỏ qua trong bản clone nông); `doctor=0`; `True`; status rỗng. Sau đó xoá `$fresh` và `release-notes-vi2.md` trong thư mục tạm (mỗi đường dẫn một lệnh; nếu bị chặn bởi kiểm tra an toàn thì bỏ qua và ghi vào báo cáo). Không xoá nhánh `feat/vi-tro-ly` (việc xoá nhánh phải hỏi chủ repo).
